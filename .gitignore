# macOS
.DS_Store

# Xcode / Swift
*.xcuserstate
xcuserdata/
DerivedData/
build/
*.xcworkspace/xcuserdata/
.swiftpm/
Package.resolved

# Dependencies
Pods/
Carthage/Build/

# Node (tooling)
node_modules/
npm-debug.log*

# Environment / secrets
.env
.env.*
*.pem
*.key

# Attendee PII — distributed to server operators out-of-band, never committed
attendees-*.csv

# Runtime state
server-data/
worker-data/
server/server-data/
worker/worker-data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs
*.log

# Local-only compose overrides (machine-specific, e.g. a host GPU/LLM endpoint)
docker-compose.override.yml
**/docker-compose.override.yml

# Editor
.vscode/
.idea/

# Superseded Kubo scaffold archive (binary)
node-ipfs-container.zip
